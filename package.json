{
  "name": "space-rally",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "./deploy.sh test",
    "deploy:test": "./deploy.sh test",
    "deploy:prod": "./deploy.sh prod",
    "deploy-server": "./deploy-server.sh test",
    "deploy-server:test": "./deploy-server.sh test",
    "deploy-server:prod": "./deploy-server.sh prod"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}