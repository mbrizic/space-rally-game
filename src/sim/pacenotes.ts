// Pacenotes were removed.
// The intended experience is a real co-driver reading the map (or future UI ideas).

export type Pacenote = never;

export function computePacenote(): null {
  return null;
}
